[package]
name = "tab2plot_lib"
version = "0.1.0"
edition = "2024"

[dependencies]
plotters = { version = "0.3.7", features = ["ttf"] }
png = "0.18.1"
serde = { version = "1.0.228", features = ["derive"] }
